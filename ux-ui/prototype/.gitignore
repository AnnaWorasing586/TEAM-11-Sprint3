# Claude Design handoff source — เก็บไว้ใน local เท่านั้น
# (เพราะมี AI-tooling artifacts ที่ไม่ได้ตั้งใจ submit)
source/NutriScan AI.dc.html
source/support.js
source/README.md

# screens/ ขึ้น GitHub ได้ — เป็น visual reference ให้ทีม ux-ui
